---
title: "Visualization"
output: 
  html_document:
    toc: FALSE
---


```{r, echo=FALSE, out.width='25%', fig.align='center'}
knitr::include_graphics(here::here("bobross_pic.png"))
```

```{r}
library(knitr)
library(tidyverse)
library(dplyr)
library(janitor)
library(here)
library(readr)
theme_set(theme_light())

```

```{r}
bob_ross <- readr::read_csv("https://raw.githubusercontent.com/rfordatascience/tidytuesday/master/data/2019/2019-08-06/bob-ross.csv")
```


```{r}
glimpse(bob_ross)
```

```{r}
 bob_ross_creations <- bob_ross %>%
  janitor::clean_names() %>%
  gather(element, present, -episode, -title) %>%
  filter(present == 1)
```

```{r}
glimpse(bob_ross_creations)
```

```{r}
bob_ross_creations %>%
  count(element, sort = TRUE) %>%
  head(25) %>%
  mutate(element = fct_reorder(element, n)) %>%
  ggplot(aes(element, n , label = scales::percent(n))) +
  geom_col(fill = "dodgerblue3") +
  coord_flip()
```

```{r}
bob_ross_creations %>%
  add_count(title) %>%
  arrange(desc(n))
```



