---
title: "Visualization"
output: 
  html_document:
    toc: FALSE
---

This will contain the future visualization exercise.
```{r, echo=FALSE}
knitr::opts_chunk$set(error = TRUE)
```


```{r}
library(knitr)
library(tidyverse)
library(dplyr)
library(lubridate)
theme_set(theme_light())

```

```{r}
bob_ross_painting <- here::here("data", "elements-by-episode_csv.csv")
```

```{r}
str(elements_by_episode_csv)
```

```{r}
element_present <- elements_by_episode_csv %>% gather(element, present, -episode, -title) %>% filter(present == 1)
```

```{r}
dplyr::glimpse(element_present)
```

```{r}
bob_ross_gathered %>%
  count(element, sort = TRUE) %>%
  head(25) %>%
  mutate(element = fct_reorder(element, n)) %>%
  ggplot(aes(element, n)) +
  geom_col() +
  coord_flip()
```